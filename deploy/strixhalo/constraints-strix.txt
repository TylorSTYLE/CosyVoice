# Hold torch/torchaudio to the gfx1151 ROCm builds already installed, and pin
# numpy to CosyVoice's required version, so installing the runtime deps can never
# swap in a CUDA/CPU torch or a newer numpy.
torch==2.9.1+rocm7.13.0rc2
torchaudio==2.9.0+rocm7.13.0rc2
numpy==1.26.4
