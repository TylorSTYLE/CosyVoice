# CosyVoice runtime deps for the Strix Halo (gfx1151) EAGER image.
# Derived from repo requirements.txt with CUDA/TensorRT/training bits stripped:
#   - torch/torchaudio      -> installed from the AMD gfx1151 ROCm index (NOT here)
#   - deepspeed             -> training-only (cosyvoice/bin/train.py); not imported at inference
#   - tensorrt-cu12*        -> only imported lazily when load_trt=True (we run eager)
#   - onnxruntime-gpu       -> replaced with CPU onnxruntime (frontend onnx runs on CPU)
#   - gradio/fastapi-cli/grpcio-tools/tensorboard -> UI/dev/train, not needed for the bench
# torch/torchaudio/numpy are held to the ROCm build via constraints-strix.txt.
conformer==0.3.2
diffusers==0.29.0
fastapi==0.115.6
gdown==5.1.0
hydra-core==1.3.2
HyperPyYAML==1.2.3
inflect==7.3.1
librosa==0.10.2
lightning==2.2.4
matplotlib==3.7.5
modelscope==1.20.0
networkx==3.1
omegaconf==2.3.0
onnx==1.16.0
onnxruntime==1.18.0
# openai-whisper (frontend imports `whisper`) is installed separately with --no-deps
# in the Dockerfile: it pins triton<3, which conflicts with ROCm torch's triton 3.5.1.
# Its real runtime deps are provided here instead; triton (from torch) is only used
# lazily by whisper's word-timestamp path (guarded), so `import whisper` is fine.
tiktoken
more-itertools
numba
tqdm
protobuf==4.25
pyarrow==18.1.0
pydantic==2.7.0
pyworld==0.3.4
rich==13.7.1
soundfile==0.12.1
transformers==4.51.3
x-transformers==2.11.24
uvicorn==0.30.0
wetext==0.0.4
wget==3.2
